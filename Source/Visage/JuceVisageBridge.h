#pragma once

#include <JuceHeader.h>

class JuceVisageBridge
{
public:
    JuceVisageBridge();
    ~JuceVisageBridge();
    
private:
    JUCE_DECLARE_NON_COPYABLE_WITH_LEAK_DETECTOR(JuceVisageBridge)
};